class Solution {
public:
    int findMaximumPairs(const string &students) {
        int x = 0, y = 0;

        for (char ch : students) {
            if (ch == 'x')
                x++;
            else if (ch == 'y')
                y++;
        }

        return min(x, y);
    }
};
